# 腾讯云轻量服务器部署指南（免费试用 1 个月）

> 全程不需要在本地敲任何命令：本地只做「双击打包」一个动作，
> 其余操作都在腾讯云网页上点按钮 + 粘贴一条命令完成。

---

## 整体思路

```
本地双击打包 → 上传到服务器网页终端 → 粘贴 1 条命令 → 得到 http://服务器IP
```

- 前端：打包好的静态文件，由服务器的 nginx 直接提供
- 后端：Node 22 进程（pm2 守护、开机自启），SQLite 数据库随包带上（**含你本机的全部数据**）
- 不需要域名、不需要备案，直接用 IP 访问

---

## 第一步：领取免费试用（0 元）

1. 打开试用专区：**https://cloud.tencent.com/act/free/personal**
   （没注册的先注册，个人实名认证后才能领）
2. 找到「**轻量应用服务器**」，推荐领这两个之一：
   - **2核4G / 5M带宽 / 60G盘 / 500G流量 · 1个月**（个企同享，带宽大，**推荐**）
   - 2核2G / 3M带宽 / 40G盘 / 200G流量 · 1个月（个人认证）
3. 点「0 元试用」时选择：
   - **地域**：广州 或 上海（离你近的）
   - **镜像**：系统镜像 → **Ubuntu 22.04**（不要选宝塔/WordPress 那类应用镜像）
   - 其余默认即可
4. 创建成功后，进入控制台：**https://console.cloud.tencent.com/lighthouse**

> 试用到期前如果想继续用，控制台里点「续费」即可（试用期间续费 1 年有折扣）。

## 第二步：放行 80 端口（防火墙）

1. 控制台 → 轻量应用服务器 → 点进你刚创建的实例
2. 切到「**防火墙**」页签
3. 看规则里有没有 **TCP : 80**：
   - 有 → 不用动
   - 没有 → 点「添加规则」→ 协议 TCP、端口 80、策略允许，确定

## 第三步：本地打包（双击一下）

1. 双击 **`E:\claude\vue-admin\deploy\pack-server.cmd`**
2. 等它跑完（约 1 分钟，会自动构建前端 + 带上数据库和图片）
3. 成功后产物在：**`E:\claude\vue-admin\deploy\out\vue-admin-server.tar.gz`**（约 8 MB）

> 以后每次改完代码要更新服务器，就重新双击一次这个文件，然后重复第四、五步。

## 第四步：上传到服务器

1. 控制台 → 实例详情 → 点「**登录**」按钮（会打开网页终端 OrcaTerm）
   - 如果提示要密码：实例页「更多 → 密码/密钥 → 重置密码」设置一个 root 密码
2. 网页终端右上角有个「**上传文件**」按钮（云朵/上传图标）
3. 选择刚生成的 `vue-admin-server.tar.gz` 上传（传到用户主目录）

## 第五步：一条命令部署

在网页终端里粘贴并回车：

```bash
cd ~ && tar xzf vue-admin-server.tar.gz && bash vue-admin-server/setup.sh
```

脚本会自动完成：装 Node 22（国内镜像，很快）→ 装 nginx + pm2 → 部署前后端 →
生成生产密钥 → 启动并设开机自启 → 最后打印：

```
  访问地址：http://你的服务器IP
```

浏览器打开这个地址即可。把这个地址发给同事就能用了。

---

## 常见问题

| 现象 | 处理 |
|---|---|
| 浏览器打不开网站 | 控制台「防火墙」没放行 TCP:80，回第二步 |
| 网页打开了但接口报错 | 网页终端执行 `pm2 logs vue-admin-backend` 看日志 |
| 想重启后端 | `pm2 restart vue-admin-backend` |
| 想看服务器 IP | 实例列表页就有「公网 IP」 |
| 更新版本 | 本地双击 pack-server.cmd → 重新上传 → 再跑一遍第五步的命令（脚本可重复执行） |
| 试用期结束 | 控制台续费，或按同样流程换一台新服务器（数据在 tar 包里，随时能迁） |

## 数据说明

- 部署包里带的是**打包那一刻**的数据库快照（产品、订单、账号全在）
- 之后服务器上产生的新数据（同事新增的订单等）存在**服务器**上，本地看不到；反之亦然
- 服务器数据想拉回本地：网页终端执行
  `cd /var/www/vue-admin/backend && tar czf ~/backup.tar.gz data uploads`
  然后用终端的「下载」按钮把 `~/backup.tar.gz` 取回来
